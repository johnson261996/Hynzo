// import 'package:shared_preferences/shared_preferences.dart';
// // ///Add all your validation codes here and call the methods as follows in your widgets :
// // ///```
// // ///Validations.emailValidation("enteredEmail")
// // ///```

// class Localstorage {
//   Localstorage._();
// SharedPreferences prefs = await SharedPreferences.getInstance();
//   // returns a error message when the field is empty or lenght is less than 3 characters
//   static String set(String number) {
//     if (number.isEmpty || number.length < 5) {
//       return Strings.PHONE_NUMBER_VALIDATION;
//     } else {
//       return null;
//     }
//   }
// }
